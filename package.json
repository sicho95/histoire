{
  "name": "histoires-pwa",
  "version": "2.4.2",
  "private": true,
  "type": "module",
  "description": "Conteur interactif pour enfants, installable et utilisable hors ligne.",
  "scripts": {
    "generate:stories": "node scripts/generate-signature-stories.mjs",
    "generate:audio": "node scripts/generate-static-audio.mjs",
    "build": "node scripts/build-static.mjs",
    "test": "node --test",
    "validate:stories": "node scripts/validate-stories.mjs",
    "validate:audio": "node scripts/validate-audio.mjs",
    "check": "npm run validate:stories && npm run validate:audio && npm test && npm run build"
  },
  "engines": {
    "node": ">=20"
  }
}
