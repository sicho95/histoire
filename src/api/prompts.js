export function buildStoryPrompt({ storyTitle, nodeTitle, transcript, choices, descendants, endings }) {
  return `Tu écris un vrai conte interactif pour enfant en français. Titre global : ${storyTitle}. Scène actuelle : ${nodeTitle}. Réponse libre de l'enfant : ${transcript}. Choix visibles actuels : ${choices.map(c => c.label).join(', ') || 'aucun'}. Nœuds de rattachement possibles : ${descendants.map(d => `${d.id}:${d.headline}`).join(', ') || 'aucun'}. Fins prévues : ${endings.map(d => `${d.id}:${d.headline}`).join(', ') || 'aucune'}. Réponds en JSON strict uniquement. Si la réponse correspond clairement à un choix existant, renvoie {"matchLabel":"mot"}. Sinon renvoie {"generated":{"label":"mot","cover_emoji":"✨","headline":"titre court","text":"vrai passage narratif de 120 à 180 mots, concret, immersif, sans jamais parler de génération, de branche, de prompt, de choix, ni de l'enfant qui invente une idée","question":"question très courte","rejoin_node_id":"id parmi les nœuds fournis ou vide","choices":[{"label":"mot1","fallback_emoji":"🌟"},{"label":"mot2","fallback_emoji":"🗺️"},{"label":"mot3","fallback_emoji":"🤝"}]}}. Le texte doit raconter la scène elle-même, pas expliquer sa création.`;
}
